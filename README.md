# laravelvueTodoApp
```markdown 
laravel laradock vue todo app

how to run backend ? : docker-compose up -d nginx redis mysql workspace ( in laradock folder )

How to run frontend ? : docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d (in laravelVueFrontend folder)
